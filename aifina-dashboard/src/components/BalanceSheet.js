import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from './UIComponents';

const BalanceSheet = () => (
  <Card>
    <CardHeader>
      <CardTitle>Balance Sheet</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Balance sheet data visualization goes here.</p>
    </CardContent>
  </Card>
);

export default BalanceSheet;